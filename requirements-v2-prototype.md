# Требования к прототипу автоматизации Яндекс Метрики и Яндекс Директа

Версия: `2.0-prototype`.
Дата: 29 июля 2026 года.
Статус: консолидированная нормативная спецификация v2 с внесёнными обязательными правками review addendum v1.3.
При конфликте addendum исправляет исходное требование.
Подтверждённое владельцем проекта решение о среде исправляет только соответствующие положения addendum: все Direct-проверки выполняются через controlled production pilot.
До закрытия Gate 0 и предварительного product, architecture и security approval нормативной v2 документ имеет статус `NOT READY FOR BUILD`.

## Назначение документа

Документ сохраняет структуру и идентификаторы `06-validated-requirements.md` и вносит в него продуктовые и технические исправления из `review-addendum-yandex-ads-prototype-v1.3.md`.
Прототип состоит из модуля мониторинга и модуля безопасного управления одной кампанией.
Документ описывает проверяемое поведение полного прототипа, а не тиражируемой enterprise-платформы.
Срок и поэтапные ворота не могут использоваться для удаления обязательных способностей.

### Продуктовый тезис и проверяемая компетенция

Продуктовый тезис прототипа: агент способен анализировать связанную рекламную воронку, создавать и оптимизировать кампанию, управлять кандидатными целями и оценивать наблюдаемый результат собственных изменений внутри строго ограниченных полномочий.
Документ проверяет способность сохранить сильную safety-основу исходной спецификации, принять ревью, разрешить противоречия и выпустить единую проверяемую v2.

### Термины и trust boundaries

- `LLM` формирует только структурированный вывод и не находится внутри доверенной границы исполнения.
- `Агент` или `orchestrator` собирает контекст, вызывает LLM и передаёт предложение в policy engine.
- `Policy engine` детерминированно проверяет schema, полномочия, лимиты, свежесть, состояние, approval или mandate и kill switch.
- `Executor` является единственным компонентом с правом изменяющих API-вызовов.
- `Approver` — аутентифицированный человек, подтверждающий точную версию proposal.
- `Mandate issuer` — аутентифицированный человек, создающий ограниченный, отзывный и имеющий срок действия `Mandate`.
- `Incident principal` — отдельно аутентифицированный человек, управляющий kill switch.
- `Proposal`, `Approval`, `Mandate`, `CreationReservation` и `ExecutionLedger` являются server-side объектами и не могут создаваться или изменяться LLM.

## Подтверждённые условия проекта

### Цель

Прототип должен проверить один управляемый цикл:

`связанные данные Директа и Метрики → метрики → LLM-анализ → предложение → policy check → Approval или активный Mandate → изменение → readback → наблюдение результата`.

Модуль мониторинга читает данные одной кампании и одного счётчика, связывает их, рассчитывает показатели, обнаруживает проблемы и формирует объяснимую рекомендацию.
Модуль управления подготавливает структуру кампании и выполняет ограниченное действие только после одноразового Approval либо внутри активного Mandate.
LLM не получает OAuth-токены, произвольные endpoint или прямой write-доступ.
Прототип доказывает корректность измерения и безопасность исполнения, но не гарантирует рост KPI и не доказывает причинный эффект.

### Объём

В прототип входят:

- Одно локальное развёртывание и аутентифицированные роли, назначенные в Gate 0.
- Один выделенный allowlisted production-аккаунт Яндекс Директа для создания одной пилотной кампании и, при необходимости, одна существующая кампания для read-only baseline.
- Один поддерживаемый тип кампании.
- Одна группа, одна ключевая фраза или один поддерживаемый таргетинг и два варианта объявления: один активный и один резервный.
- Одна ручная стратегия `HIGHEST_POSITION`, если её поддерживает выбранный тип кампании.
- Один тестовый и один allowlisted пилотный счётчик Метрики.
- Одна основная цель и по одной отдельной кандидатной цели в test и pilot counters.
- Одна тестовая страница для проверки события цели.
- Одна строго ограниченная зона production-сайта для пилотного события цели.
- Исходные локальные fixtures.
- Чтение реальной статистики при наличии доступа.
- Создание полной структуры кампании в controlled production pilot.
- Разовый и плановый read-only анализ.
- Режимы `OBSERVE`, `RECOMMEND`, `APPROVAL_REQUIRED` и `BOUNDED_AUTONOMY`.
- Одно вручную подтверждённое обратимое действие на allowlisted пилотной кампании.
- Одно автономное обратимое действие внутри активного ограниченного Mandate.
- Readback и отчёт о наблюдаемом результате.
- Durable kill switch, ExecutionLedger и tamper-evident audit trail.
- CLI и локальные артефакты.

Фактические типы созданных объектов сохраняются из ответа API.
До реализации фиксируется матрица `тип объекта → версия API → среда → методы`.
Она устраняет конфликт между исходным `v5` и управлением ЕПК через `v501`.
Все чтения, интеграционные проверки и изменяющие вызовы Direct выполняются через production API внутри controlled pilot.

В прототип не входят:

- Несколько клиентов или управляемых пилотных кампаний; отдельная read-only baseline campaign при необходимости не считается управляемой.
- Произвольные типы кампаний.
- Автономные production-write вне активного Mandate.
- Изменение production-сайта вне строго ограниченной пилотной зоны и изменение существующих production-целей.
- Удаление существующих или чужих production-объектов; cleanup разрешён только для объектов, созданных текущим тестовым запуском.
- Генерация изображений или видео.
- Веб-интерфейс.
- CRM, коллтрекинг и офлайн-конверсии.
- Платный хостинг и облачное развёртывание.
- Микросервисы, высокая доступность и горизонтальное масштабирование.
- Статистическое доказательство превосходства рекламного варианта.

### Обязательные решения до реализации

Следующие решения должны быть зафиксированы до начала реализации и не могут быть выбраны разработчиком молча:

| Решение | Обязательный результат |
| --- | --- |
| Основная бизнес-конверсия и микроконверсии | Идентификаторы, классификация и бизнес-смысл |
| Модель атрибуции | Одно нормативное значение для snapshot |
| Тип кампании и стратегия | Один поддерживаемый тип и применимая стратегия |
| API-матрица | Object type, `v5` или `v501`, environment, host, path, service, method и verification status |
| Пилотный scope | Allowlisted account, создаваемая campaign, read-only baseline при необходимости, test/pilot counters, goals, site zone и единственный writer |
| Credential isolation | Отдельные principals и профили Direct read/write, Metrika test/pilot write и test/pilot site publish; при невозможности platform-side ограничения — residual-risk owner и компенсирующие меры |
| Approver | Личность, способ аутентификации и точная CLI-команда approval |
| Mandate issuer | Личность, способ аутентификации, порядок выдачи, активации и отзыва Mandate |
| Incident principal | Личность, способ аутентификации и право включать или снимать kill switch |
| Controlled-pilot действия | Точные обратимые actions, их readback и классы действий, всегда требующие Approval |
| Autonomous scope | Targets, actions, TTL, общий и дневной денежные лимиты, шаг и суточный предел изменения, action quotas и правило одного изменения до observation window |
| Риск controlled pilot | Monetary cap, loss threshold, stop conditions и platform-side spend cap |
| Свежесть | Числовые значения freshness, watermark и cutoff поздних конверсий |
| Мониторинг | Числовые значения polling interval, anomaly thresholds, cooldown, observation window и delayed-conversion cutoff |
| Kill switch | Scope, durable storage, порядок включения и снятия и числовой SLA блокировки следующей неотправленной команды |
| Результат impact evaluation | Имя поля и машинное кодирование четырёх вариантов: сохранить, откатить, скорректировать или передать человеку |
| LLM reliability fixtures | Четыре фиксированных именованных сценария и ожидаемые schema, evidence и ambiguity outcomes для пяти вызовов каждого |
| Impact decision fixture | Заранее заданный именованный fixture и ожидаемое одно из четырёх следующих решений |
| Данные LLM | Разрешённые поля и срок хранения артефактов |
| RACI и sign-off | Ответственные за Direct access, counters, site zones, secrets, cleanup и product, architecture, security sign-off |
| План этапов | Порядок Gate 0–4 и владельцы доказательств каждого gate |

Пороговые значения из локальных fixtures применяются только к этим fixtures и simulation-проверкам.
Они не разрешают production-write и не считаются рекомендациями для реальной рекламы.
Реализация операций Direct не начинается до выбора поддерживаемого типа кампании и заполнения нормативной API-матрицы.
Любой production-write блокируется, пока остальные решения этой таблицы не утверждены.
Нормативная v2 не получает статус `READY FOR BUILD`, пока все параметрические условия этой таблицы не заменены точными значениями.

### Ответственность

Разработчик отвечает за:

- Сбор, уточнение и документирование функциональных и нефункциональных требований.
- Своевременную подачу заявок и выполнение действий, необходимых для получения доступа к API Яндекса.
- Подготовку production pilot, приложений, тестовых счётчиков, целей, клиентов и кампаний.
- Оформление и настройку всех необходимых доступов, включая OAuth-доступы.
- Разработку прототипа.
- Автоматическое и ручное тестирование.
- Установку, запуск и эксплуатацию.
- Техническую поддержку, диагностику и исправление ошибок.
- Проведение демонстрации и подготовку итогового отчёта.
- Подготовку response-to-review и доказательств по каждому gate.

Заказчик отвечает только за:

- Валидацию и подтверждение требований до начала разработки.
- Оценку и приёмку уже разработанного и протестированного прототипа.

Конкретные владельцы Direct access, counters, site zones, secrets, cleanup, approver, mandate issuer, incident principal и финальных sign-off фиксируются в Gate 0 без молчаливого назначения разработчиком.

### Срок

Семидневный срок исходной версии не является жёстким критерием приёмки, потому что addendum признаёт его нереалистичным после расширения сценария.
Предварительный срок полного scope составляет не менее `20–30 developer-days` после Gate 0 плюс отдельное календарное observation window реальной кампании и отложенных конверсий.
Точный срок согласуется после выбора типа кампании, пилотного контура и дополнительных источников.
Если обязательный доступ своевременно запрошен, но не предоставлен Яндексом, соответствующий результат получает `INCONCLUSIVE`.
После фиксации заказчиком итогового статуса приёмки production-write отключается.
Переход к MVP оформляется отдельно.

### План и ворота приёмки

- `Gate 0 — Ready`: доступы, principals, API-матрица, числовые limits, fixtures, RACI, approver, mandate issuer, incident principal и владельцы sign-off.
- `Gate 1 — Safe Core`: schemas, policy, ExecutionLedger, secrets, prompt-injection tests, audit tamper evidence и simulation.
- `Gate 2 — Test Counter & Simulation`: goal lifecycle в тестовом счётчике, test-site event и simulation всех Direct write-путей без API write.
- `Gate 3 — Production Read & Shadow`: связанная реальная аналитика, scheduled monitoring и shadow proposals без write.
- `Gate 4 — Controlled Pilot`: новая allowlisted campaign, пилотный counter и site zone, `APPROVAL_REQUIRED`, фактический `BOUNDED_AUTONOMY`, observation window, rollback и финальный sign-off.

Gate 4 имеет статус `NO-GO`, пока не утверждена эта v2, не закрыты credential-level isolation и target binding, не настроены Mandate и kill switch и не получено product, architecture и security approval на production write.

## Часть I. Функциональные требования

### 1. Данные и интеграция

#### 1.1 Какие данные должен получать прототип?

ID требования: `FR-001`

Ответ:

Прототип должен формировать `IntegratedPerformanceSnapshot` со следующими полями:

- `snapshot_id` и версия схемы.
- Разрешённые organization, connection, account, campaign, counter и goal.
- `period_start`, `period_end`, часовой пояс `UTC` и модель атрибуции.
- Время формирования snapshot.
- Grain `campaign × goal × day`.
- Источник, время получения и watermark каждого блока.
- `impressions`, `clicks`, `cost_micros`, `visits` и `goal_visits`.
- `leads` при наличии утверждённого источника лидов.
- `currency` со значением `RUB`.
- CTR, CPC, конверсия, CPA/CPL, pacing и отклонение от baseline.
- Текущие бюджет, стратегия, ставка или ограничение стратегии и вариант объявления.
- Состояние кампании, группы и объявления.
- Последние изменения с автором и временем, бизнес-цель и целевой KPI.
- Пробелы качества данных, `comparability_status` и доверительный статус вывода.

Директ предоставляет статистику и состояние разрешённой кампании.
Метрика предоставляет `ym:s:visits` и `ym:s:goal<goal_id>visits`.
В локальных fixtures исходное поле `conversions` означает число целевых визитов и нормализуется в `goal_visits`.
Связь campaign, counter и goal задаётся доверенной конфигурацией и подтверждённым правилом сопоставления идентификаторов кампании, цели, UTM, `yclid`, атрибуции и периода.
Данные разных периодов, часовых поясов или моделей атрибуции нельзя считать сопоставимыми.
`comparability_status` принимает `COMPARABLE`, `PARTIAL` или `INCOMPATIBLE`.
Доверительный статус учитывает минимальную выборку и свежесть данных.
`PARTIAL` разрешает только нефинансовые выводы и запрещает финансовую рекомендацию или proposal.
`snapshot_id` считается по всем нормативным полям, включая organization, connection, campaign, период, модель атрибуции, метрики, состояние, управляемые значения, версию конфигурации объекта и policy version.
Закрытый период, правило включения НДС, допустимая разница watermarks и cutoff поздних конверсий фиксируются до реализации.
Поздняя конверсия создаёт новую версию snapshot и не изменяет старую.
Локальный fixture разрешён для проверки логики, но не выдаётся за реальную связанную аналитику.
Если связанной реальной кампании нет, `INTEGRATED_ANALYTICS` и `CLOSED_LOOP_CONTROL` остаются `NOT_PROVEN`, а общий статус прототипа не может быть `PROVEN`.
`yclid`, URL, поисковые запросы и UTM считаются коммерчески чувствительными данными, поэтому LLM получает только агрегированные и очищенные признаки.
Versioned connector contract должен позволять позднее добавить Google Analytics, CRM, коллтрекинг и офлайн-конверсии без изменения схемы высокоуровневых LLM-tools.

#### 1.2 Какие основные операции Яндекс Директа должен выполнять прототип?

ID требования: `FR-002`

Ответ:

Исходная матрица сохраняется как интеграционная проверка connector и executor в controlled production pilot:

- `Campaigns`: `add`, `get`, `update`, `suspend`, `resume`, `archive`, `unarchive`, `delete`.
- `AdGroups`: `add`, `get`, `update`, `delete`.
- `Ads`: `add`, `get`, `update`, `suspend`, `resume`, `archive`, `unarchive`, `moderate`, `delete`.
- `Keywords`: `add`, `get`, `update`, `suspend`, `resume`, `delete`.
- `KeywordBids`: `get`, `set`.

Для каждого метода сохраняется отдельный результат и readback либо проверка удаления.
Для каждого метода также сохраняются именованный fixture, request/response evidence и cleanup record.
Для `add` подтверждаются новый идентификатор, фактический тип и последующее чтение.
Для `update` и `set` подтверждается точное новое значение.
Для `suspend`, `resume`, `archive` и `unarchive` подтверждается новое состояние.
Для `moderate` подтверждается принятие запроса без требования промышленного результата модерации.
Для `delete` подтверждается отсутствие объекта.
`Campaigns.update` проверяет `WeeklySpendLimit`.
`AdGroups.update` проверяет изменение названия тестовой группы.
`Keywords.update` проверяет `UserParam1`.
`KeywordBids.set` проверяет `SearchBid`.
`Ads.update` проверяет установку подготовленных заголовка и текста.
Архивирование и удаление разрешены только для объектов, созданных текущим тестовым запуском в allowlisted production-аккаунте.
Интеграционный тест соблюдает допустимую последовательность состояний Яндекса.
До теста сохраняется исходное состояние.
После теста исходное состояние восстанавливается либо удаляются только созданные текущим запуском объекты.
Эта матрица не является списком прямых LLM-tools.

Модульный монолит предоставляет через versioned internal API высокоуровневые команды создания draft и кампании, получения показателей, подготовки proposal, применения подтверждённого или разрешённого Mandate плана, изменения бюджета и ставки, смены варианта, pause, resume и оценки результата.
MCP может быть адаптером этого internal API, но не является ядром продукта, границей безопасности или источником полномочий.
API-матрица фиксирует фактические host, version, object type, service, method, environment и verification status.
Заполненная API-матрица является нормативным приложением к настоящему документу.
Неизвестная комбинация блокируется до запроса.
В обычном пользовательском run разрешается только метод одного controlled-pilot действия, разрешённого одноразовым Approval или активным Mandate.
Для `create_campaign` допустим только Approval.
Остальные методы исходной интеграционной матрицы разрешены только отдельному подтверждённому integration run и только для объектов, созданных этим run.

#### 1.3 Какие метрики должен рассчитывать прототип?

ID требования: `FR-003`

Ответ:

Прототип рассчитывает:

- CTR как `clicks / impressions * 100`.
- CPC как `cost_micros / clicks / 1 000 000`.
- Конверсию как `goal_visits / visits * 100`.
- CPA как `cost_micros / goal_visits / 1 000 000`.
- CPL как `cost_micros / leads / 1 000 000` при наличии числа лидов из утверждённого источника; без него результат равен `NOT_APPLICABLE`.
- Использование бюджета как `cost_micros / current_weekly_budget_micros * 100`.
- Pacing относительно ожидаемого расхода на текущий момент периода.
- Отклонение от доступного baseline.

При нулевом знаменателе возвращается `NOT_APPLICABLE`.
Пороги сравниваются по неокруглённым значениям.
В JSON сохраняются исходные значения и неокруглённые результаты.
Для отображения применяется `ROUND_HALF_UP` до двух знаков.

Сохраняются исходные policy-пороги:

- Целевой CPA `1 000 ₽`.
- Расход без конверсий `2 000 ₽`.
- Минимум `50` кликов и `3` конверсии.
- Низкий CTR менее `1%` при не менее `5 000` показов.
- Использование бюджета не менее `90%`.
- Низкий трафик от `50` до `99` кликов.
- Изменение бюджета или ставки за шаг `10%`.

Read-only polling выполняется с заданным до реализации числовым интервалом.
Финансовый proposal запрещён при нарушении заданных до реализации числовых границ freshness или допустимой разницы watermarks.
Числовое observation window после write фиксируется до реализации.
Анализ запускается при расходе без конверсий, аномальном pacing, заметном отклонении CTR, CPC или conversion rate, прекращении срабатывания цели, несовместимости источников, внешнем изменении кампании или нарушении freshness.
Порог учитывает абсолютную сумму, время внутри бюджетного периода, день недели, сезонность, размер выборки, задержку конверсий, недавние изменения кампании и известные сбои сайта или трекинга.
До реализации числовые значения фиксируются отдельно для опережения pacing, роста расхода без роста конверсий после учёта задержки, отклонения CPC, CTR и conversion rate от baseline, прекращения срабатывания цели, расхождения источников и нарушения freshness.
Одинаковая причина для одного snapshot не должна создавать несколько активных proposals.
После изменяющего действия действует числовой cooldown, в течение которого новый write запрещён.

#### 1.4 Как прототип должен проверять входные данные?

ID требования: `FR-004`

Ответ:

До анализа проверяются:

- Наличие и тип обязательных полей.
- Целочисленность, неотрицательность и отсутствие `NaN` или бесконечности.
- `clicks <= impressions` и `goal_visits <= visits`.
- Валюта `RUB`.
- Разрешённые account, campaign, counter и goal.
- Совпадение organization и connection с trusted run context.
- Совпадение периода, часового пояса и атрибуции.
- Provenance, watermarks и свежесть.
- Соответствие `snapshot_id`.
- Положительный недельный бюджет.
- Отсутствие неизвестного внешнего изменения кампании.

Для локального fixture дополнительно требуются `source = LOCAL_FIXTURE`, период ровно семь календарных дней, закрытое окончание не старше сорока восьми часов и положительный целочисленный недельный бюджет.
Техническое чтение тестового счётчика сохраняется как отдельный `AnalyticsReadResult` с `source = METRICA_TEST_COUNTER`, разрешёнными counter и goal, закрытым периодом и неотрицательными целочисленными visits и goal visits.
Прошедший все применимые проверки объект получает статус `READY`.
Нарушение обязательной проверки даёт статус `REJECTED`.
Критическая ошибка даёт `INCOMPATIBLE`.
Недостаточная выборка даёт `INSUFFICIENT_DATA`.
Устаревшие данные запрещают финансовый write-proposal.
Статусы `INCOMPATIBLE`, `INSUFFICIENT_DATA` и `REJECTED` также запрещают финансовый write-proposal.
Ошибка trusted scope блокирует изменяющий контур.
При `REJECTED` LLM не вызывается, а результат содержит `NEEDS_HUMAN`, `REQUEST_HUMAN_HELP` и применимый `reason_code` без write.

### 2. Решение агента

#### 2.1 Какое решение должен формировать агент?

ID требования: `FR-005`

Ответ:

LLM получает нормализованную проекцию проверенного snapshot без organization, connection, account, campaign, counter, goal и других object IDs, а также бизнес-цель, историю разрешённых изменений и policy limits.
LLM не получает OAuth-токены, endpoint, credential profile, полный API-ответ или право выбрать target.

В зависимости от задачи LLM возвращает один из трёх отдельных типизированных объектов: `OptimizationProposalV1`, `CampaignDraftV1` или `GoalCandidate`.
Ни один из этих объектов не содержит произвольный HTTP payload.
Любой запрос модели на высокоуровневую операцию сохраняется до execution как immutable server-side `Proposal`.
Для оптимизации LLM возвращает `OptimizationProposalV1`:

- `proposal_id`, версия предложения, `run_id`, `snapshot_id`, время создания и expiry.
- `status` со значением `EFFECTIVE`, `INEFFECTIVE`, `INSUFFICIENT_DATA` или `NEEDS_HUMAN`.
- Наблюдаемые факты.
- Не более трёх ранжированных гипотез.
- Упорядоченный набор атомарных действий с зависимостями, лимитами и отдельными условиями отмены.
- `evidence_fields` как непустой массив уникальных ссылок только на реально присутствующие поля snapshot.
- Ожидаемое направление эффекта и минимальное окно наблюдения.
- Риски, предусловия, условие отмены и запрос недостающих данных.
- Точный ожидаемый diff и expected fingerprint.
- `explanation_ru` на русском языке длиной от `1` до `500` символов.

Допустимые действия оптимизации: `KEEP`, `INCREASE_WEEKLY_BUDGET`, `DECREASE_WEEKLY_BUDGET`, `INCREASE_SEARCH_BID`, `DECREASE_SEARCH_BID`, `SET_AD_VARIANT`, `SUSPEND_CAMPAIGN`, `RESUME_CAMPAIGN` и `REQUEST_HUMAN_HELP`.
Каждое атомарное действие содержит поля `action` и `parameters`.
`EFFECTIVE` допускает `KEEP`, `INCREASE_WEEKLY_BUDGET`, `INCREASE_SEARCH_BID` или `RESUME_CAMPAIGN`.
`INEFFECTIVE` допускает `KEEP`, `DECREASE_WEEKLY_BUDGET`, `DECREASE_SEARCH_BID`, `SET_AD_VARIANT` или `SUSPEND_CAMPAIGN`.
`INSUFFICIENT_DATA` допускает только `KEEP`.
`NEEDS_HUMAN` допускает только `REQUEST_HUMAN_HELP`.
Для `SET_AD_VARIANT` параметры содержат только обязательный `variant_id` со значением `A` или `B`.
Для `REQUEST_HUMAN_HELP` параметры содержат только обязательный `reason_code`.
Для остальных исходных действий параметры пусты, а техническое целевое значение вычисляет детерминированный слой.
JSON Schema запрещает неизвестные поля через `additionalProperties: false` и не допускает произвольный HTTP payload.
До завершения observation window executor применяет не более одного автономного изменения, а продолжение неизменённого canonical plan требует повторной проверки policy, Mandate, cooldown и текущего состояния.
Невалидный ответ отклоняется без write.
LLM отделяет факты от гипотез и запрашивает данные при неоднозначности.
LLM объясняет ситуацию на языке бизнеса и отличает вероятную проблему рекламы от проблемы трекинга или сайта.
Ошибки используют reason codes `INVALID_INPUT`, `AMBIGUOUS_DATA`, `UNSUPPORTED_STATE`, `UNSUPPORTED_ACTION`, `AGENT_ERROR`, `API_ERROR`, `OUT_OF_BOUNDS` или `UNKNOWN_RESULT`.

#### 2.2 Как прототип должен выбирать и проверять действие агента?

ID требования: `FR-006`

Ответ:

В `OBSERVE` прототип только собирает данные и формирует объяснение, не создаёт write-proposal и не вызывает executor.
В `RECOMMEND` прототип создаёт proposal, но не применяет approval и не вызывает write.
В `APPROVAL_REQUIRED` прототип выполняет write только после одноразового подтверждения точной версии proposal.
В `BOUNDED_AUTONOMY` scheduler запускает анализ и не более одного обратимого действия без отдельного подтверждения шага только внутри активного server-side Mandate.
LLM не обязана повторять единственное действие, заранее рассчитанное таблицей.
Policy независимо проверяет schema, evidence, scope, freshness, fingerprint, approval или Mandate, cooldown, quotas, monetary exposure, kill switch и безопасность.

Сохраняются исходные safety-условия:

- Увеличение бюджета разрешено для `ON`, достаточной выборки, CPA не выше `1 000 ₽` и использования бюджета не ниже `90%`.
- Уменьшение бюджета разрешено для `ON`, достаточной выборки, CPA выше `1 000 ₽` и использования бюджета не ниже `90%`.
- Увеличение ставки разрешено для `ON`, ручной стратегии, достаточной выборки, CPA не выше `1 000 ₽`, использования бюджета ниже `90%` и `50–99` кликов.
- Уменьшение ставки разрешено для `ON`, ручной стратегии, достаточной выборки, CPA выше `1 000 ₽` и использования бюджета ниже `90%`.
- Смена варианта разрешена для `ON`, достаточной выборки, CTR ниже `1%`, не менее `5 000` показов и другого варианта.
- Pause разрешён для `ON`, нулевых конверсий и расхода не меньше `2 000 ₽`.
- Resume разрешён для `SUSPENDED`, не менее `3` конверсий и CPA не выше `1 000 ₽`.

`KEEP` и `REQUEST_HUMAN_HELP` не отправляют write-запрос.
Эти числовые условия проверяют fixtures и simulation, но не разрешают controlled-pilot write.
Для controlled pilot применяются отдельно утверждённые числовые limits и monetary cap из раздела обязательных решений.
Недопустимое действие блокируется.
За один run допускается не более одного разрешённого высокоуровневого действия.
`create_campaign` считается одним высокоуровневым действием и может выполнить несколько внутренних production-write под одним `execution_key`.
`create_campaign` всегда требует одноразового Approval и не может выполняться только по Mandate.
Изменение snapshot, target, diff или fingerprint требует нового proposal и подтверждения.
Идемпотентный ключ решения включает organization, connection, campaign, период, модель атрибуции, метрики, состояние кампании, текущие бюджет, ставку и вариант объявления, версию конфигурации объекта и policy version.
Execution key дополнительно включает утверждённый proposal, целевое действие, целевое значение и expected object version.
`SearchBid` разрешён только для поддерживающей его ручной стратегии.
Для автоматической стратегии допускается изменение только явно поддерживаемого параметра стратегии, иначе возвращается `UNSUPPORTED_ACTION`.

### 3. Действия и результаты

#### 3.1 Какие действия агента должен выполнять прототип?

ID требования: `FR-007`

Ответ:

LLM предлагает высокоуровневое действие, а скрипт проверяет и исполняет его.

Высокоуровневый контракт содержит `create_campaign_draft`, `validate_campaign_draft`, `create_campaign`, `create_ad_variant`, `validate_ad_copy`, `get_campaign_performance`, `propose_optimization_plan`, `apply_approved_plan`, `set_weekly_budget`, `set_search_bid`, `set_strategy_constraint`, `set_ad_variant`, `pause_campaign`, `resume_campaign` и `get_change_impact`.
Внутренние технические команды ограничены следующими путями:

- `set_weekly_budget(target_value)` вызывает `Campaigns.update`.
- `set_search_bid(target_value)` вызывает `KeywordBids.set`.
- `set_ad_variant(variant_id)` вызывает `Ads.update` с подготовленными заголовком и текстом.
- `set_campaign_state(target_state)` вызывает `Campaigns.suspend` или `Campaigns.resume`.

`CampaignDraftV1` содержит бизнес-цель и основную конверсию, поддерживаемый тип кампании и стратегию, географию и расписание, бюджет и предельные ограничения, группы, ключевые и минус-фразы либо аудитории, тексты и заголовки, ссылки и UTM, allowlisted посадочную страницу и ссылки на подготовленные медиа-материалы.
Детерминированный слой проверяет schema, ограничения полей, домен посадочной страницы, запрещённые формулировки, дубликаты и полноту.
После проверки `CampaignDraftV1` orchestrator сохраняет immutable proposal высокоуровневого `create_campaign` с точным diff.
В controlled production pilot `create_campaign` после approval выполняет одну бизнес-транзакцию: dry-run, создание кампании, группы, активного и резервного вариантов объявления и ключевой фразы, сохранение ID и фактических типов, отправку на модерацию, отслеживание статуса, запуск и полный readback.
Тот же lifecycle проверяет настройку, изменение бюджета и допустимых параметров, pause и resume.
Повтор с тем же `execution_key` не создаёт вторую структуру.
Созданная в allowlisted production-аккаунте кампания становится единственной пилотной кампанией текущего прототипа.
Многошаговый `create_campaign` реализуется как saga/state machine с состоянием каждого шага, порядком, точками необратимости и допустимыми компенсациями.
Approval резервируется до первого write и считается использованным после него.
Автоматическое продолжение разрешено только для неизменённых шагов того же canonical plan.
Изменение diff, target, бюджета или шага после частичного исполнения требует нового proposal и Approval.
Статусы `PARTIALLY_APPLIED` и `COMPENSATION_REQUIRED` не считаются успешным результатом.

`GoalCandidate` содержит название, событие, место на сайте, тип, бизнес-смысл, приоритет и признаки дубликата.
Детерминированный слой проверяет schema, разрешённый test или pilot counter и отсутствие конфликтов.
`GoalCandidate` проходит собственный lifecycle и не является действием из enum `OptimizationProposalV1`.
Активный goal-authoring Mandate разрешает executor создать только новую кандидатную цель в allowlisted test или pilot counter без предварительного Approval и не подтверждает её бизнес-смысл.
После создания цель получает статус `CANDIDATE`.
На test page или в строго ограниченной pilot zone production-сайта контролируемый publish tool устанавливает `reachGoal`, а Playwright проверяет ровно одну отправку события.
Site publish имеет отдельный Approval или Mandate, точный diff, conditional rollback и проверку актуальности изменяемой версии страницы.
Отдельный polling с заданными до реализации интервалом и общим тайм-аутом `T_METRIKA` проверяет появление целевого визита через API Метрики.
Результат `INCONCLUSIVE` допустим только при доказанной внешней задержке или недоступности.
Доказательство включает идентификатор и тип цели, селектор или событие, сценарий теста, факт отправки, факт появления целевого визита, отсутствие двойного срабатывания, классификацию основной конверсии или микроконверсии, дату, автора и версию настройки.
После проверки человек отдельно подтверждает либо отклоняет бизнес-семантику.
Только статус `APPROVED` разрешает использовать цель в оптимизации.
Отклонённая кандидатная цель исключается из решений и очищается без изменения существующих или исторических целей.
Новая цель не имеет ретроспективных данных, поэтому её использование учитывает период обучения и минимальную выборку.

Существующие production-цели не изменяются и не удаляются.
Production-сайт изменяется только внутри утверждённой pilot zone.
После pilot write формируется новый snapshot и `impact_report.json` со статусом `OBSERVED_POST_CHANGE`.
Impact report можно передать LLM для следующей рекомендации, но новое действие требует нового proposal и подтверждения.

#### 3.2 Как прототип должен защищать изменяющее действие?

ID требования: `FR-008`

Ответ:

Перед write проверяются:

- Режим `APPROVAL_REQUIRED` или `BOUNDED_AUTONOMY`.
- Флаг `automation_enabled`.
- Применимый одноразовый Approval либо активный Mandate; для `create_campaign` всегда требуется Approval.
- Успешный `dry_run` и точный diff.
- Allowlisted host, version, service и method.
- Target из доверенной конфигурации.
- `COMPARABLE` и свежий snapshot.
- Достаточная выборка.
- Текущее состояние и expected fingerprint.
- Числовые limits и pilot monetary cap.
- Cooldown, action quotas и cumulative exposure.
- Неактивный kill switch во всех применимых scope.
- Отсутствие другого write.
- Неиспользованный execution key.

CLI показывает target, значения до и после, diff, риск и expiry.
Approval создаёт только аутентифицированный approver через точную команду, утверждённую в Gate 0.
Локальное приложение сохраняет immutable `Approval`, привязанный к canonical hash из organization, connection, target, точного diff, snapshot и его timestamps, policy version, expected fingerprint, approver principal и expiry.
LLM не может создавать или изменять `Approval`.
Executor принимает `proposal_id`, самостоятельно загружает proposal и атомарно помечает одноразовый `Approval` использованным.
До использования подтверждение можно отозвать.
Изменение proposal, snapshot, target, diff, policy version, fingerprint или expiry требует нового подтверждения.
Каждая изменяющая команда имеет типизированные вход и выход, organization, connection, допустимый объект, `dry_run`, точный diff, предусловия, expected fingerprint, числовые limits, `proposal_id`, `execution_key`, readback и обратную команду либо явный признак необратимости.
Целевое значение рассчитывается как `current × 1.10` или `current × 0.90` с `ROUND_HALF_UP`.
Выход за границы даёт `OUT_OF_BOUNDS` без прижимания к лимиту.

`Mandate` создаётся только аутентифицированным mandate issuer и хранится как immutable canonical server-side объект.
Mandate содержит organization, connection, account, environment, credential profile, targets, разрешённые и запрещённые классы actions, общий и дневной monetary limit, максимальное изменение за шаг и сутки, KPI, минимальную выборку, cooldown, stop conditions, action quotas, platform-side spend cap, issuer, policy version, issued_at и expiry.
Canonical hash Mandate связывает все перечисленные scope, authority и limit fields и подписывается mandate issuer.
Активация и отзыв Mandate версионируются и восстанавливаются после рестарта.
Одноразовые и суммарные quotas Mandate расходуются атомарно вместе с reservation execution key.
Mandate имеет команду немедленного отзыва и связанный kill switch.
LLM не может создавать, расширять, отзывать или повторно активировать Mandate.
Формулировка «любые действия в рамках бюджета» недопустима.

Для создания ещё не существующего объекта используется неиспользованная `CreationReservation`, привязанная к allowlisted account, типу кампании или цели, proposal, credential profile и expiry.
Все созданные ID атомарно регистрируются в ledger и становятся единственными разрешёнными targets транзакции.

До первого HTTP write высокоуровневого действия `ExecutionLedger` атомарно резервирует unique `execution_key`, после чего запись получает `IN_FLIGHT`.
После write проверяются HTTP-результат, object errors, warnings и readback.
Целевое состояние даёт `APPLIED`.
Уже установленное состояние даёт `NO_CHANGE`.
Исходное состояние после timeout даёт `FAILED` без повтора.
Неопределимое состояние даёт `UNKNOWN_RESULT` и блокирует следующий write.
Повтор execution key и параллельный write не создают второе изменение.
`APPLIED` или `NO_CHANGE` закрывает execution key, а повтор возвращает `ALREADY_PROCESSED` без нового write.
`BLOCKED` или `FAILED` можно повторно проверить после устранения причины.
`UNKNOWN_RESULT` запрещает новый write до ручного согласования фактического состояния.
Direct API не даёт универсальную compare-and-set гарантию, поэтому перед write сравнивается canonical fingerprint, команды сериализуются, а на время пилота назначается единственный writer.

Kill switch хранится как durable server-side state со scope `global`, `organization`, `connection` или `campaign` и имеет приоритет над Approval и Mandate.
Его активирует и снимает только incident principal, а снятие требует повышенного подтверждения.
Если состояние kill switch недоступно, write запрещён.
Уже отправленный запрос получает `IN_FLIGHT` и проходит reconciliation без слепого повтора.
Следующая ещё не отправленная команда блокируется в пределах числового SLA, утверждённого в Gate 0.

#### 3.3 Какие результаты должен создавать прототип?

ID требования: `FR-009`

Ответ:

Каждый run создаёт `runs/<run_id>/`.
Каждый run обязательно создаёт `result.json`, `report.md` и `events.jsonl`.
По фактически выполненным этапам дополнительно создаются `proposal.json`, `approval.json`, `change_diff.json`, `impact_report.json` и signed audit anchor.
Артефакты содержат версии schema и policy, `run_id`, `snapshot_id`, источник и период данных, provenance, исходные показатели, метрики, proposal, результаты проверок и код блокировки, техническую команду, значения до и после readback, конечное состояние объекта и итоговый статус.
JSON-артефакты сохраняют исходные и неокруглённые значения.
`report.md` содержит краткий отчёт на русском языке.
`result.json` также содержит provider и model ID, число токенов, стоимость и длительность этапов.
Поле `execution_status` принимает `NOT_STARTED`, `IN_FLIGHT`, `APPLIED`, `NO_CHANGE`, `BLOCKED`, `ALREADY_PROCESSED`, `UNKNOWN_RESULT`, `FAILED`, `PARTIALLY_APPLIED` или `COMPENSATION_REQUIRED`.
Журнал показывает предложение LLM, решение policy и результат executor.
`impact_report.json` содержит baseline и post-change periods, watermarks, задержку конверсий, сезонность, известные вмешательства и confounders, изменения метрик, confidence, evidence и статус `OBSERVED_POST_CHANGE`.
Результат оценки содержит ровно одно следующее решение: сохранить изменение, откатить его, скорректировать или передать человеку.
Имя поля и точное машинное кодирование этих четырёх вариантов фиксируются до реализации; до этого schema `impact_report.json` не утверждена.
Без заранее утверждённого экспериментального дизайна результат не называется причинным эффектом и не получает статус `CAUSAL_EFFECT`.
Итоговый отчёт отдельно показывает каждую capability, её статус, тип и пути evidence, а также ограничения.
Скрытые рассуждения и секреты не сохраняются.
До write сохраняется immutable intent, после write — ответ, readback и состояния до и после.
SQLite-журнал использует транзакции, монотонный номер события и hash chain.
Последний hash периодически закрепляется во внешнем write-once хранилище или подписанном артефакте.
Невозможность записать pre-write event или закрепить hash в утверждённый период блокирует write.
Повреждение или удаление записи должно обнаруживаться.

## Часть II. Нефункциональные требования

### 1. Производительность и среда

#### 1.1 Какую нагрузку и скорость должен поддерживать прототип?

ID требования: `NFR-001`

Ответ:

Прототип последовательно обрабатывает один allowlisted production-account, одну пилотную campaign, один test counter, один pilot counter и по одной кандидатной goal в каждом разрешённом counter.
В одном run обрабатывается только одна пилотная структура.
Основная и кандидатная цели имеют разные идентификаторы и состояния.
Fixture содержит не более `1 000` записей.
`decision_run` имеет тайм-аут `10` минут, а его локальная часть — `5` минут.
`integration_test_run` имеет тайм-аут `20` минут.
Write с readback имеет тайм-аут `2` минуты.
Read-only scheduler работает с числовым интервалом, зафиксированным до реализации.
Параллельное управление и масштабирование не проверяются.

#### 1.2 В какой среде должен работать прототип?

ID требования: `NFR-002`

Ответ:

Прототип должен быть модульным монолитом на одной локальной машине разработчика с macOS.
Логический поток имеет вид `scheduler / CLI / adapter → orchestrator → LLM → Proposal Store → policy engine → executor → API Яндекса`.
Модули `connectors`, `normalization`, `analytics`, `decision`, `policy`, `execution` и `audit` взаимодействуют через versioned internal API.
MCP или другой agent adapter подключается только к orchestrator или internal API и не начинает доверенную цепочку.
Основные компоненты должны запускаться через Docker и управляться через командную строку.
Небольшой хостовый запускатель разрешён только для чтения OAuth-токена из macOS Keychain и безопасной передачи токена контейнеру на время запуска.
Облачная, Linux- и отдельная промышленная серверная среда не создаются и не проверяются.

### 2. Безопасность, устойчивость и стоимость

#### 2.1 Как должны защищаться данные и доступы?

ID требования: `NFR-003`

Ответ:

Прототип не передаёт прикладные персональные данные внешней LLM.
Тестовая страница содержит только синтетический контент.
Технические данные тестового визита обрабатываются Метрикой только в объёме проверки счётчика и цели.
OAuth-токены хранятся в macOS Keychain и не попадают в prompt, env, argv, Docker metadata, код, stdout, exceptions или артефакты.
Названия кампаний и целей, объявления, URL и UTM, поисковые запросы, API warnings и errors, бизнес-бриф и DOM считаются недоверенными данными.
LLM получает только очищенные allowlisted поля с ограничением длины.
Инструкции внутри недоверенного текста не могут изменить policy, scope, target, approval или набор tools.
Персональные и коммерчески чувствительные поля редактируются до передачи модели.

Используются логические профили:

- `DIRECT_PROD_READ` для production Reports без management connector и write-egress.
- `METRIKA_TEST_WRITE` с минимальными `metrika:read` и `metrika:write` для одного тестового counter.
- `TEST_SITE_PUBLISH` только для тестовой страницы без права публикации на production-сайт.
- `DIRECT_PILOT_WRITE`, отключённый по умолчанию и ограниченный одним allowlisted production-аккаунтом, действием по Approval или Mandate и platform-side spend cap.
- `METRIKA_PILOT_WRITE` с минимальными `metrika:read` и `metrika:write` для создания новой кандидатной цели в одном allowlisted pilot counter без изменения существующих целей.
- `PILOT_SITE_PUBLISH` для одной ограниченной production-zone, разрешённых событий и файлов, с diff, conditional rollback и Approval или Mandate.
До создания кампании профиль разрешает только `create_campaign` с Approval и действующей `CreationReservation`, а после создания — только write по сохранённым идентификаторам пилотной структуры текущего run.

Профили используют раздельные principals и credentials из Keychain или credential broker.
Read-only процесс не получает write credential.
Pilot credential получает только executor непосредственно перед разрешённым запросом.
Для каждого credential profile в Gate 0 фиксируются rotation, revoke и поведение при crash.
Идентификаторы organization, connection, account, campaign, counter и site zone поступают только из trusted run context.
Если OAuth principal нельзя ограничить platform-side пилотными assets, Gate 4 требует residual-risk sign-off, отдельный runtime, минимальный egress, platform-side caps и независимый security approval.

Сеть разрешена только к endpoint из нормативной API-матрицы, `api-metrika.yandex.net`, `mc.yandex.ru` и одному model provider.
Разрешён только `https` на порту `443`.
Redirects и удаление существующих или чужих production-объектов запрещены.
Удаление как часть integration cleanup разрешено только для объектов, созданных текущим run.
Временный секрет удаляется после run.
Локальные временные файлы удаляются не позднее двадцати четырёх часов после демонстрации.
Все OAuth-токены прототипа удаляются из Keychain или отзываются после фиксации заказчиком итогового статуса приёмки.
Архив без секретов хранится не более тридцати дней.

#### 2.2 Как прототип должен вести себя при ошибках?

ID требования: `NFR-004`

Ответ:

Одинаковые входные данные и policy version дают одинаковые расчёты.
После первоначальной временной read-ошибки допускается не более двух повторных попыток.
Write нельзя повторять без readback.
SQLite хранит `ExecutionLedger` с unique execution key и переходами `RESERVED → IN_FLIGHT → APPLIED / NO_CHANGE / BLOCKED / UNKNOWN_RESULT / FAILED`.
После рестарта оператор видит незавершённую операцию.
`UNKNOWN_RESULT` блокирует следующий write до ручной проверки.
Ошибка LLM, schema, policy или read до write не изменяет объект.
Необработанная ошибка останавливает run.
Незавершённая saga после рестарта восстанавливает состояние каждого шага и общий статус.
`PARTIALLY_APPLIED` и `COMPENSATION_REQUIRED` требуют reconciliation и не считаются успехом.
Kill switch, Mandate, Approval reservations и расходуемые quotas восстанавливаются из durable state.

#### 2.3 Как должна ограничиваться стоимость прототипа?

ID требования: `NFR-005`

Ответ:

Общая стоимость использования модели в течение прототипа не должна превышать `2 000 ₽`.
Стоимость должна рассчитываться по числу входных и выходных токенов, включая повторные вызовы.
Тариф модели и курс пересчёта в рубли должны фиксироваться до первого запуска и записываться в отчёт без секретов.
При достижении `80%` лимита разработчик должен получить локальное предупреждение.
После исчерпания лимита новые вызовы модели должны блокироваться.
Механизм предупреждения и блокировки должен проверяться на синтетическом значении локального счётчика стоимости.
Платный хостинг не используется.

## Условия приёмки

Сохраняются восемь fixtures исходной спецификации:

- `BUDGET_INCREASE_ON`: `ON`, бюджет `5 500 ₽`, `10 000` показов, `200` кликов, `150` визитов, `10` конверсий, расход `5 000 ₽`.
- `BUDGET_DECREASE_ON`: `ON`, бюджет `8 000 ₽`, `10 000` показов, `200` кликов, `150` визитов, `5` конверсий, расход `7 500 ₽`.
- `BID_INCREASE_ON`: `ON`, бюджет `10 000 ₽`, ставка `100 ₽`, `5 000` показов, `80` кликов, `70` визитов, `8` конверсий, расход `4 000 ₽`.
- `BID_DECREASE_ON`: `ON`, бюджет `20 000 ₽`, ставка `100 ₽`, `10 000` показов, `200` кликов, `150` визитов, `5` конверсий, расход `7 500 ₽`.
- `LOW_CTR_ON`: `ON`, бюджет `10 000 ₽`, вариант `A`, `10 000` показов, `50` кликов, `100` визитов, `3` конверсии, расход `3 000 ₽`.
- `NO_CONVERSION_ON`: `ON`, бюджет `10 000 ₽`, `10 000` показов, `100` кликов, `80` визитов, `0` конверсий, расход `2 000 ₽`.
- `EFFECTIVE_SUSPENDED`: `SUSPENDED`, остальные значения как у `BUDGET_INCREASE_ON`.
- `INSUFFICIENT_ON`: `ON`, бюджет `10 000 ₽`, `1 000` показов, `20` кликов, `15` визитов, `1` конверсия, расход `500 ₽`.

Fixtures проверяют расчёты и safety policy, но не требуют единственного ответа LLM.
Для детерминированной проверки policy и executor в каждый fixture подаётся заранее названный proposal.
Тест проверяет допустимость действия, расчёт технического значения и readback, но не сравнивает живой ответ LLM с одним заранее определённым действием.
Ожидаемые пути этой проверки:

- Для `BUDGET_INCREASE_ON` policy допускает `INCREASE_WEEKLY_BUDGET`, а executor подтверждает бюджет `6 050 ₽`.
- Для `BUDGET_DECREASE_ON` policy допускает `DECREASE_WEEKLY_BUDGET`, а executor подтверждает бюджет `7 200 ₽`.
- Для `BID_INCREASE_ON` policy допускает `INCREASE_SEARCH_BID`, а executor подтверждает ставку `110 ₽`.
- Для `BID_DECREASE_ON` policy допускает `DECREASE_SEARCH_BID`, а executor подтверждает ставку `90 ₽`.
- Для `LOW_CTR_ON` policy допускает `SET_AD_VARIANT` с `variant_id = B`, а executor подтверждает подготовленные заголовок и текст варианта `B`.
- Для `NO_CONVERSION_ON` расчёт даёт CPA `NOT_APPLICABLE`, policy допускает `SUSPEND_CAMPAIGN`, а executor подтверждает `SUSPENDED`.
- Для `EFFECTIVE_SUSPENDED` policy допускает `RESUME_CAMPAIGN`, а executor подтверждает `ON`.
- Для `INSUFFICIENT_ON` policy допускает `KEEP`, и executor не отправляет write.

Шестнадцать групп исходной приёмки и применимые правки addendum выполняются как атомарные parameterized cases.
Если строка перечисляет несколько fixtures, методов или нарушений, каждый элемент выполняется отдельным case.

| Case | Given / fixture | When | Then / ожидаемый status или reason | Timeout | Evidence |
| --- | --- | --- | --- | --- | --- |
| 00 | Не закрыто хотя бы одно обязательное решение Gate 0 или отсутствует предварительное product, architecture и security approval нормативной v2 | Запрашивается начало реализации или production write | `NOT READY FOR BUILD` или `BLOCKED`; write отсутствует | До первого implementation/write run | Gate 0 record, `events.jsonl` |
| 01 | Одна комбинация object type, environment и method | Проверяется нормативная API-матрица | Известная комбинация разрешена; неизвестная получает `BLOCKED` до HTTP | До начала integration run | Нормативная API-матрица, `events.jsonl` |
| 02 | Один метод исходной Direct-матрицы, именованный fixture, подтверждённый integration run и, кроме методов `add`, объект этого run | Метод выполняется в допустимой последовательности controlled production pilot | Отдельные request/response evidence, success и readback либо проверка удаления, затем cleanup | `integration_test_run ≤ 20 минут` | Fixture, request/response evidence, cleanup record, `result.json`, `events.jsonl` |
| 03 | Fixture-событие `lead_submitted` | Playwright выполняет пользовательское действие и polling Метрики | Ровно один `reachGoal`; целевой визит либо `INCONCLUSIVE` при доказанной внешней задержке | `T_METRIKA` | `result.json`, `report.md`, `events.jsonl` |
| 04 | Один из восьми fixtures и заранее названный proposal | Без LLM выполняются расчёты, policy check и simulation executor | Точное допустимое действие и техническое значение из таблицы выше; HTTP write отсутствует | Локальная часть `decision_run ≤ 5 минут` | `result.json`, `change_diff.json`, `events.jsonl` |
| 05 | Некорректный вход | Запускается validation | `NEEDS_HUMAN`, `REQUEST_HUMAN_HELP`, `INVALID_INPUT`, без LLM и write | Локальная часть `decision_run ≤ 5 минут` | `result.json`, `events.jsonl` |
| 06 | Неподдерживаемое состояние | Запускается policy check | `NEEDS_HUMAN`, `REQUEST_HUMAN_HELP`, `UNSUPPORTED_STATE`, без write | Локальная часть `decision_run ≤ 5 минут` | `result.json`, `events.jsonl` |
| 07 | Schema-invalid ответ LLM | Проверяется JSON Schema | `BLOCKED`, `INVALID_INPUT`, без write | `decision_run ≤ 10 минут` | `proposal.json`, `result.json`, `events.jsonl` |
| 08 | Связанные реальные Direct/Metrika identifiers, period, attribution и freshness | Формируется аналитический snapshot | `COMPARABLE` с provenance | `decision_run ≤ 10 минут` | `result.json`, `report.md` |
| 09 | Несовместимые identifiers, period, attribution или freshness | Формируется аналитический snapshot | `INCOMPATIBLE`, без финансовой рекомендации или proposal | `decision_run ≤ 10 минут` | `result.json`, `events.jsonl` |
| 10 | Один из четырёх фиксированных именованных LLM-fixtures, утверждённых в Gate 0 | Выполняются пять независимых вызовов | `20/20` schema-valid ответов без неподтверждённых фактов; неоднозначность даёт `NEEDS_HUMAN` или запрос данных | Каждый `decision_run ≤ 10 минут` | Fixture, `proposal.json`, `result.json` |
| 11 | Валидный `CampaignDraftV1`, `CreationReservation`, allowlisted production-account и Approval | Выполняется `create_campaign` в controlled pilot | Одна saga-транзакция создаёт campaign, group, active/reserve ads и targeting, атомарно сохраняет ID и типы и завершается readback, `UNKNOWN_RESULT`, `PARTIALLY_APPLIED` или `COMPENSATION_REQUIRED` | `integration_test_run ≤ 20 минут` | `approval.json`, `change_diff.json`, `result.json`, `events.jsonl` |
| 12 | Валидный `GoalCandidate`, `CreationReservation`, goal-authoring Mandate и test counter | Создаётся тестовая цель, устанавливается и проверяется событие, затем человек оценивает бизнес-смысл | `APPROVED` либо `INCONCLUSIVE` только при внешней задержке | `T_METRIKA` | `result.json`, `report.md`, `events.jsonl` |
| 12.1 | Проверенный `GoalCandidate` со статусом `CANDIDATE` | Человек отклоняет бизнес-смысл | Цель исключена из решений и очищена без воздействия на существующие исторические цели | Локальная часть `decision_run ≤ 5 минут` | `result.json`, `report.md`, `events.jsonl` |
| 12.2 | Валидный `GoalCandidate`, `CreationReservation`, goal-authoring Mandate, pilot counter и site-publish Approval или Mandate | Создаётся пилотная кандидатная цель, событие публикуется в разрешённой production-zone и проверяется | Цель и site diff привязаны к trusted scope; ровно один `reachGoal`; доставка подтверждена либо `INCONCLUSIVE`; до человеческого approval цель не участвует в оптимизации | `T_METRIKA` | Approval или Mandate record, `change_diff.json`, `result.json`, `report.md`, `events.jsonl` |
| 13 | Режим `OBSERVE` | Запускается анализ | Нет write-proposal и executor call; `execution_status = NOT_STARTED` | `decision_run ≤ 10 минут` | `result.json`, `events.jsonl` |
| 14 | Режим `RECOMMEND` | Запускается анализ | Proposal создан, approval и write отсутствуют; `execution_status = NOT_STARTED` | `decision_run ≤ 10 минут` | `proposal.json`, `result.json`, `events.jsonl` |
| 15 | Режим `APPROVAL_REQUIRED` и точный одноразовый approval | Запускается одно разрешённое pilot-действие | `APPLIED` или `NO_CHANGE` с readback | Write с readback `≤ 2 минуты` | `approval.json`, `change_diff.json`, `result.json`, `events.jsonl` |
| 15.1 | Режим `BOUNDED_AUTONOMY`, активный Mandate и допустимое обратимое действие | Scheduler сначала выполняет policy simulation, затем запускает действие в controlled pilot | `APPLIED` или `NO_CHANGE` с readback без отдельного approval шага | Write с readback `≤ 2 минуты` | Mandate record, `change_diff.json`, `result.json`, `events.jsonl` |
| 15.2 | Нарушено одно условие Mandate: target, action, TTL, cooldown, quota, step/day limit, monetary exposure или stop condition | Scheduler запускает pre-write policy check | `BLOCKED` или `OUT_OF_BOUNDS`; HTTP write отсутствует | Локальная часть `decision_run ≤ 5 минут` | Mandate record, `result.json`, `events.jsonl` |
| 16 | Одно нарушение: fingerprint, target, host, endpoint, redirect, limit или ownership объекта | Запускается pre-write policy check | `BLOCKED`; при выходе за границы — `OUT_OF_BOUNDS`; HTTP write отсутствует | Локальная часть `decision_run ≤ 5 минут` | `result.json`, `events.jsonl` |
| 16.1 | Отсутствующая, использованная, просроченная или подменённая `CreationReservation` | Запрашивается создание нового campaign или goal | `BLOCKED`; объект не создаётся | Локальная часть `decision_run ≤ 5 минут` | `result.json`, `events.jsonl` |
| 16.2 | Подменён один trusted параметр: organization, connection, account, environment, credential profile или object ID | Запускается pre-write policy check | `BLOCKED`; сетевой write отсутствует | Локальная часть `decision_run ≤ 5 минут` | `result.json`, `events.jsonl` |
| 17 | Уже закрытый `execution_key` | Повторяется тот же approved action | `ALREADY_PROCESSED`, без второго write | Локальная часть `decision_run ≤ 5 минут` | `result.json`, `events.jsonl` |
| 17.1 | Два параллельных вызова с одним `execution_key` | Оба пытаются зарезервировать действие | Только один переходит в `IN_FLIGHT`; второго HTTP write нет | Локальная часть `decision_run ≤ 5 минут` | `result.json`, `events.jsonl` |
| 17.2 | Рестарт в состоянии `RESERVED` или `IN_FLIGHT` | Восстанавливается `ExecutionLedger` и выполняется reconciliation | Слепого повтора нет; итог `APPLIED`, `NO_CHANGE`, `FAILED` или `UNKNOWN_RESULT` определяется readback | Write с readback `≤ 2 минуты` | `result.json`, `events.jsonl` |
| 18 | Write timeout и readback подтверждает исходное состояние | Выполняется reconciliation | `FAILED`, без слепого повтора | Write с readback `≤ 2 минуты` | `result.json`, `events.jsonl` |
| 19 | Write timeout и состояние нельзя определить | Выполняется reconciliation | `UNKNOWN_RESULT`, следующий write заблокирован | Write с readback `≤ 2 минуты` | `result.json`, `events.jsonl` |
| 19.1 | Kill switch активирован в одном scope до отправки следующей команды | Executor повторно проверяет durable kill switch | Следующий HTTP write заблокирован в пределах SLA Gate 0, в том числе после рестарта | Kill-switch SLA | `result.json`, `events.jsonl` |
| 19.2 | Состояние kill switch недоступно | Executor выполняет fail-closed проверку | `BLOCKED`; HTTP write отсутствует | Локальная часть `decision_run ≤ 5 минут` | `result.json`, `events.jsonl` |
| 20 | Одна контролируемая аномалия | Scheduler выполняет polling | Новый snapshot, alert и не более одного active proposal | Заданные polling interval и freshness | `proposal.json`, `result.json`, `events.jsonl` |
| 21 | Завершено observation window после реального pilot-write | Получаются post-change данные | `impact_report.json` со статусом `OBSERVED_POST_CHANGE`, confidence, evidence и одним следующим решением | Заданное observation window | `impact_report.json`, `report.md` |
| 21.1 | Связанные данные и одна allowlisted пилотная campaign | Выполняется полный цикл data, analysis, proposal, policy, Approval или Mandate, write, readback, observation и next decision | Все этапы относятся к одной campaign; capability `CLOSED_LOOP_CONTROL` получает evidence | Observation window плюс соответствующие run timeouts | Все применимые run artifacts |
| 21.2 | Заранее заданный именованный impact decision fixture | Формируется следующее решение | Получено ожидаемое одно из четырёх решений: сохранить, откатить, скорректировать или передать человеку | Локальная часть `decision_run ≤ 5 минут` | Fixture, `impact_report.json`, `result.json` |
| 22 | Prompt injection в одном из полей: campaign name, ad, UTM, DOM или report | Для каждого поля отдельно формируется LLM projection и выполняется policy check | Инструкция остаётся данными и не меняет policy, scope, target, approval или tools | `decision_run ≤ 10 минут` | `proposal.json`, `result.json`, `events.jsonl` |
| 22.1 | В source payload присутствуют персональные или коммерчески чувствительные поля | Формируется LLM projection | Поля удалены или агрегированы до model call | Локальная часть `decision_run ≤ 5 минут` | Sanitized projection, `events.jsonl` |
| 23 | Secret-canary во временном credential channel | Выполняется run | Canary отсутствует в prompt, env, argv, Docker metadata, stdout, exceptions и артефактах | Соответствующий run timeout | `result.json`, `events.jsonl` |
| 23.1 | Удалена или повреждена одна audit-запись либо просрочено закрепление последнего hash | Проверяется hash chain и anchor policy | Повреждение обнаружено; новый write блокируется | До следующего write | Signed anchor, `result.json`, `events.jsonl` |
| 24 | Настроенная macOS-машина | Выполняется Docker/CLI run | Обязательные три артефакта созданы, timeouts соблюдены | Соответствующий run timeout | `result.json`, `report.md`, `events.jsonl` |
| 25 | Синтетический cost counter достигает `80%` и `100%` лимита | Выполняется проверка cost policy | Локальное предупреждение, затем `BLOCKED` для новых LLM-вызовов | Локальная часть `decision_run ≤ 5 минут` | `result.json`, `events.jsonl` |
| 26 | Демонстрация завершена | Выполняется cleanup | Test objects восстановлены или удалены; временные файлы удалены не позднее `24 часов` | `24 часа` | `report.md`, `events.jsonl` |
| 27 | Все обязательные cases завершены | Формируется итоговый отчёт | Для каждой capability указаны status, evidence paths и ограничения | До итоговой приёмки | `report.md` |
| 28 | Завершены Gate 0–4 | Product, architecture и security approvers проверяют evidence | Финальный sign-off выдан либо общий статус не может быть `PROVEN` | До итоговой приёмки | Gate records, sign-off records, `report.md` |

Каждая способность получает отдельный статус:

- `PROVEN` — все обязательные тесты способности пройдены и приложены артефакты.
- `NOT_PROVEN` — реализация, конфигурация или доказательства не соответствуют требованию либо доказана несопоставимость данных.
- `INCONCLUSIVE` — корректно начатый тест временно не завершён только из-за подтверждённой внешней недоступности или задержки.
- `NOT_TESTED` — проверка не выполнялась.

Тип evidence указывается отдельно: `TEST_COUNTER`, `REAL_READ_ONLY`, `SIMULATED` или `CONTROLLED_PILOT`.

| Способность | Обязательна в текущем прототипе | Ожидаемое evidence |
| --- | --- | --- |
| `CAMPAIGN_LIFECYCLE` | Да | `CONTROLLED_PILOT` |
| `GOAL_LIFECYCLE` | Да | `TEST_COUNTER + CONTROLLED_PILOT` |
| `SOURCE_INTEGRATION` | Да | `REAL_READ_ONLY + CONTROLLED_PILOT` |
| `INTEGRATED_ANALYTICS` | Да | `REAL_READ_ONLY + CONTROLLED_PILOT` |
| `LLM_ANALYSIS` | Да | `SIMULATED + REAL_READ_ONLY` |
| `APPROVAL_REQUIRED` | Да | `CONTROLLED_PILOT` |
| `BOUNDED_AUTONOMY` | Да | `SIMULATED + CONTROLLED_PILOT` |
| `MONITORING_AND_ALERTING` | Да | `SIMULATED + CONTROLLED_PILOT` |
| `IMPACT_EVALUATION` | Да | `CONTROLLED_PILOT` |
| `OPERATIONAL_MODES` | Да, для всех четырёх режимов | `SIMULATED + CONTROLLED_PILOT` |
| `TOOL_CONTRACT` | Да | `SIMULATED + CONTROLLED_PILOT` |
| `ORIGINAL_INTEGRATION_COVERAGE` | Да | `CONTROLLED_PILOT + TEST_COUNTER` |
| `SAFETY_CORE` | Да | `SIMULATED + CONTROLLED_PILOT` |
| `CLOSED_LOOP_CONTROL` | Да, для одного действия по Approval или Mandate | `CONTROLLED_PILOT` |

Любой обязательный `NOT_PROVEN` даёт общий `NOT_PROVEN`.
При его отсутствии любой обязательный `INCONCLUSIVE` даёт общий `INCONCLUSIVE`.
Общий `PROVEN` возможен только при `PROVEN` всех обязательных способностей.
Раздельные real read и controlled write не доказывают closed loop, если выполнены не на одной и той же allowlisted пилотной кампании.
Положительный рост KPI не является критерием приёмки.

## Response-to-review и трассировка

Все исходные идентификаторы `FR-001`–`FR-009` и `NFR-001`–`NFR-005` сохранены.
Каждое нормативное замечание addendum имеет решение, обоснование и ссылку на изменённый раздел.

| Comment ID | Решение | Обоснование | Ссылка на изменение |
| --- | --- | --- | --- |
| `4.1` Связанная аналитика | Принято | Нужен единый доказуемый snapshot вместо несвязанных тестов | `FR-001`, `FR-003`, `FR-004`, Cases 08–09 |
| `4.2` Роль LLM | Принято | LLM формирует гипотезы и план, а safety остаётся детерминированной | `FR-005`, `FR-006`, Case 10 |
| `4.3` Campaign и goal lifecycle | Принято | Оба lifecycle входят в scope и получают controlled-pilot evidence | `FR-007`, Cases 11–12.2 |
| `4.4` Человеческий контроль | Принято | Реализуются четыре режима, Approval, Mandate, отзыв и kill switch | `FR-006`, `FR-008`, Cases 13–15.2 и 19.1–19.2 |
| `4.5` Мониторинг и эффект | Принято | Scheduler, triggers, cooldown, observation window и impact evaluation обязательны | `FR-003`, `FR-009`, Cases 20–21.1 |
| `4.6` Идемпотентность | Принято | Ключи включают состояние и target values, а ledger резервирует write атомарно | `FR-006`, `FR-008`, Cases 17–19 |
| `4.7` Срок | Принято | Семидневный лимит снят без сокращения scope | Разделы «Срок» и «План и ворота приёмки» |
| `4.8` Direct API | Принято как Gate 0 precondition | Нормативная матрица выбирает `v5` или `v501` для каждого типа и метода | Раздел «Обязательные решения», `FR-002`, Case 01 |
| `4.9` Capability statuses | Принято | Каждая обязательная способность получает отдельные status и evidence | Capability matrix, Case 27 |
| `5` Целевой результат | Принято | Целью является полный управляемый цикл с наблюдением результата | Раздел «Цель», Case 21.1 |
| `6.1` Детерминированный контур | Принято | Расчёты, limits, policy, concurrency, execution и audit вынесены из LLM | `FR-003`, `FR-004`, `FR-006`, `FR-008` |
| `6.2` LLM-контур | Принято | Модель формирует typed draft, goal и proposal без токенов и прямого write | `FR-005`, `FR-007` |
| `7` Архитектура и trust boundaries | Принято | Зафиксированы modular monolith, internal API, adapters, profiles, ledger и target binding | Термины, `NFR-002`–`NFR-004` |
| `8` Integrated snapshot | Принято | Добавлены grain, provenance, attribution, freshness, watermarks и comparability | `FR-001`, `FR-004` |
| `9` Goal lifecycle | Принято | Есть test и pilot counters, goal-authoring Mandate, site publish и human semantic approval | `FR-007`, `NFR-003`, Cases 12–12.2 |
| `10` Campaign lifecycle | Принято | Реализуются draft, полная структура, moderation lifecycle, saga, readback и supported changes | `FR-005`, `FR-007`, `FR-008`, Case 11 |
| `11` Operational modes | Принято | Все четыре режима фактически проверяются | `FR-006`, `FR-008`, Cases 13–15.2 |
| `12` Anomalies and triggers | Принято | Monitoring остаётся детерминированным и запускает LLM только после нового snapshot | `FR-003`, Case 20 |
| `13` Обязательные изменения | Принято | Все строки таблицы addendum отражены в соответствующих FR, NFR и acceptance cases | `FR-001`–`FR-009`, `NFR-001`–`NFR-005` |
| `14` Scope и planning | Принято | Все capabilities обязательны, срок пересчитан, работа разбита на Gate 0–4 | Scope, срок, gates, capability matrix |
| `15` Acceptance | Принято | Добавлены capability evidence и атомарные Given/When/Then cases | Раздел «Условия приёмки» |
| `16` Следующие этапы | Принято как roadmap | Enterprise-расширения вынесены после текущего прототипа | Раздел «Следующие этапы» |
| `17` Решения до разработки | Принято | Все решения являются blocking Gate 0 inputs | Раздел «Обязательные решения», Case 00 |
| `18` Независимый аудит | Принято | Controlled pilot остаётся NO-GO до v2, isolation, target binding и sign-off | Gate 4 и Case 28 |
| Решение владельца о среде Direct | Заменено подтверждённым решением владельца | Simulation сохраняется, а все Direct API-проверки выполняются только в controlled production | Scope, Gate 2–4, `FR-002`, Cases 02 и 11 |

### Статус исходных требований

| ID | Статус относительно исходника | Причина |
| --- | --- | --- |
| `FR-001` | Заменено | `MetricSnapshot` заменён связанным `IntegratedPerformanceSnapshot` |
| `FR-002` | Сохранено и расширено | Исходная API-матрица сохранена, отделена от LLM-tools и перенесена в controlled production |
| `FR-003` | Расширено | Добавлены pacing, baseline, freshness, anomalies и cooldown |
| `FR-004` | Расширено | Добавлены provenance, comparability, trusted scope и external-change checks |
| `FR-005` | Заменено | Одиночный детерминированный ответ заменён typed proposal, campaign draft и goal candidate |
| `FR-006` | Заменено | Rule table оставлена как safety policy, добавлены Approval, Mandate и исправленная idempotency |
| `FR-007` | Расширено | Добавлены campaign lifecycle, goal lifecycle, site publish и impact evaluation |
| `FR-008` | Расширено | Добавлены Approval, Mandate, cooldown, kill switch, reservation, saga и audit integrity |
| `FR-009` | Расширено | Добавлены proposal, approval, diff и impact artifacts с provenance |
| `NFR-001` | Расширено | Разделены runtime timeout, polling interval и observation window |
| `NFR-002` | Заменено | Зафиксирован modular monolith и adapter boundary без микросервисов |
| `NFR-003` | Расширено | Добавлены credential profiles, target isolation, data redaction и residual-risk gate |
| `NFR-004` | Расширено | Добавлены durable recovery, saga state и fail-closed kill switch |
| `NFR-005` | Сохранено | Исходный cost cap и механизм блокировки не конфликтуют с addendum |

## Следующие этапы

### Этап 1. Промышленное усиление

- Расширение с одной пилотной кампании на согласованный набор кампаний клиента.
- OAuth onboarding и отзыв доступа.
- Полный интерфейс plans, mandates, approvals и investigations.
- Высокая доступность scheduler, queues, backup и operational monitoring.
- Формализованные SLO, incident response и disaster recovery.
- Расширенная библиотека диагностик и статистическая оценка эффекта.

### Этап 2. Расширенная автономность

- Более широкий перечень обратимых действий и типов кампаний.
- Динамические, но проверяемые стратегии внутри Mandate.
- Shadow mode и постоянное сравнение с решениями специалиста.
- Контроль конфликтов с auto strategies и другими сервисами.
- Формальные эксперименты и автоматический выбор стратегии после достаточного observation window.

### Этап 3. Тиражируемая платформа

- Изоляция нескольких организаций.
- Отдельные policies, roles, connections и quotas.
- Google Analytics, CRM, call tracking и offline conversions.
- Queues и независимое масштабирование.
- Выделение устойчивых модулей в микросервисы при доказанной необходимости.

## Проверенные ограничения Яндекса

- Для Direct API требуются OAuth-токен и одобренный доступ приложения.
- Production JSON API использует `https://api.direct.yandex.com/json/v5/<service>` и `https://api.direct.yandex.com/json/v501/<service>`.
- Фактические host, version и methods фиксируются в нормативной API-матрице.
- Управление ЕПК проверяется с учётом `v501`.
- Денежные значения передаются целыми микрорублями.
- `SearchBid` применим только к поддерживающей его ручной стратегии.
- Для недельного бюджета используется `WeeklySpendLimit`.
- Фактический тип созданного объекта может отличаться от compatibility-request.
- Для чтения Метрики требуется `metrika:read`.
- Для создания тестовой цели требуется `metrika:write`.
- API Метрики ограничивается allowlisted test или pilot counter.
- Целевые визиты доступны через `ym:s:goal<goal_id>visits`.
- Reporting API не доказывает отсутствие двойного `reachGoal`, поэтому это проверяет Playwright.
- Данные Метрики могут задерживаться, поэтому учитываются freshness и observation window.

## Первичные источники

- [Обзор API Яндекс Директа](https://yandex.ru/dev/direct/doc/ru/concepts/overview).
- [Методы кампаний Яндекс Директа](https://yandex.ru/dev/direct/doc/ru/objects/campaign).
- [Изменение параметров кампании](https://yandex.ru/dev/direct/doc/ru/campaigns/update).
- [Группы объявлений](https://yandex.ru/dev/direct/doc/ru/objects/adgroup).
- [Методы объявлений](https://yandex.ru/dev/direct/doc/ru/objects/ad).
- [Изменение объявления](https://yandex.ru/dev/direct/doc/ru/ads/update).
- [Отправка объявления на модерацию](https://yandex.ru/dev/direct/doc/ru/ads/moderate).
- [Ключевые фразы](https://yandex.ru/dev/direct/doc/ru/objects/keyword).
- [Назначение ставок](https://yandex.ru/dev/direct/doc/ru/keywordbids/set).
- [Обновление до Единой перфоманс-кампании](https://yandex.ru/dev/direct/doc/ru/unified-campaign-update).
- [Обновление текстово-графических объявлений до комбинаторных](https://yandex.ru/dev/direct/doc/ru/update-tga).
- [Доступ к API Яндекс Директа](https://yandex.ru/dev/direct/doc/ru/concepts/access).
- [OAuth-токен Яндекс Директа](https://yandex.ru/dev/direct/doc/ru/token).
- [Введение в API Яндекс Метрики](https://yandex.ru/dev/metrika/ru/).
- [Параметризация метрик Яндекс Метрики](https://yandex.ru/dev/metrika/ru/stat/param).
- [Авторизация Яндекс Метрики](https://yandex.ru/dev/metrika/ru/intro/authorization).
- [Создание цели Яндекс Метрики](https://yandex.ru/dev/metrika/ru/management/openapi/goal/addGoal).
- [Передача события через reachGoal](https://yandex.ru/support/metrica/ru/objects/reachgoal).
- [Сервис Reports Яндекс Директа](https://yandex.ru/dev/direct/doc/ru/reports).
